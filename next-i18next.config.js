/** @type {import('next-i18next').UserConfig} */
const config = {
  i18n: {
    defaultLocale: 'de',
    locales: ['en', 'de']
  },
  defaultNS: 'common',
  localePath: './public/locales',
}

export default config 